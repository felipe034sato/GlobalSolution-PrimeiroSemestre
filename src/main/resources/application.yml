server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orbitalwatch
    username: root
    password: Lipe@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

springdoc:
  swagger-ui:
    path: /

  api-docs:
    path: /api-docs